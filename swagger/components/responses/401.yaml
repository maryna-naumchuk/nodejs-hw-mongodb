description: Unauthorized
content:
  application/json:
    schema:
      type: object
      properties:
        status:
          type: integer
          example: 401
        message:
          type: string
        data:
          type: object
          properties:
            message:
              type: string
    example:
      status: 401
      message: 'Session not found'
      data:
        message: 'Session not found'
