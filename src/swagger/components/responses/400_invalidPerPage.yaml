description: Bad Request
content:
  application/json:
    schema:
      type: object
      properties:
        status:
          type: integer
          example: 400
        message:
          type: string
        data:
          type: object
          properties:
            message:
              type: string
    example:
      status: 400
      message: "The 'perPage' value cannot be more than 100."
      data:
        message: "The 'perPage' value cannot be more than 100."
